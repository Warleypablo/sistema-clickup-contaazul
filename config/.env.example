# Configurações do Banco de Dados PostgreSQL
# Para desenvolvimento local, configure estas variáveis
# Para produção no Render, use apenas DATABASE_URL

PG_HOST=seu_host_postgresql
PG_PORT=5432
PG_DBNAME=seu_banco_de_dados
PG_USER=seu_usuario
PG_PASSWORD=sua_senha

# Para produção no Render (substitui as variáveis acima)
# DATABASE_URL=postgresql://usuario:senha@host:porta/banco