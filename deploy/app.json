{
  "name": "API Conta Azul - Contas a Receber",
  "description": "Aplicação web para consulta de contas a receber extraídos do Conta Azul, filtrando clientes inadimplentes.",
  "repository": "https://github.com/seu-usuario/API_CONTAAZUL-Conexao",
  "keywords": ["python", "flask", "contaazul", "financeiro"],
  "env": {
    "FLASK_ENV": {
      "description": "Ambiente da aplicação Flask",
      "value": "production"
    },
    "PG_HOST": {
      "description": "Host do banco de dados PostgreSQL",
      "required": false
    },
    "PG_PORT": {
      "description": "Porta do banco de dados PostgreSQL",
      "required": false,
      "value": "5432"
    },
    "PG_DBNAME": {
      "description": "Nome do banco de dados PostgreSQL",
      "required": false
    },
    "PG_USER": {
      "description": "Usuário do banco de dados PostgreSQL",
      "required": false
    },
    "PG_PASSWORD": {
      "description": "Senha do banco de dados PostgreSQL",
      "required": false
    }
  },
  "addons": [
    {
      "plan": "heroku-postgresql:hobby-dev",
      "as": "DATABASE"
    }
  ],
  "buildpacks": [
    {
      "url": "heroku/python"
    }
  ],
  "scripts": {
    "postdeploy": "python init_db.py"
  }
}